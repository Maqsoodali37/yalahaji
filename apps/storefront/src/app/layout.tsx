// Root layout — required by Next.js App Router.
// next-intl's [locale]/layout.tsx handles the actual HTML structure.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
